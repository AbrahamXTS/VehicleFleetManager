pipeline {
    agent any
    stages {
        stage('Stop active containers') {
            steps {
                sh 'docker compose down'
            }
        }
        stage('Deploy') {
            steps {
                sh 'docker compose up -d'
            }
        }
    }
}